using Microsoft.UI.Xaml.Controls;

namespace WinIconFinder.Controls;

/// <summary>
/// Toolbar and menu glyph for the "copy as XAML PathIcon" actions.
/// </summary>
public sealed partial class VectorPathIcon : PathIcon
{
    public VectorPathIcon()
    {
        InitializeComponent();
    }
}
